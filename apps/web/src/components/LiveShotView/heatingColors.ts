// Per-sensor color identity shared by the heating graph and numbers blocks.
// CSS-var references only (no raw hex) so inline styles stay token-based.
export const HEAD_COLOR = 'var(--chart-1)'
export const CHAMBER_COLOR = 'var(--chart-3)'
