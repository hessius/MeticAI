name: Build macOS Installer

on:
  push:
    paths:
      - 'macos-installer/**'
      - 'local-install.sh'
      - 'uninstall.sh'
    branches:
      - main
      - develop
  pull_request:
    paths:
      - 'macos-installer/**'
      - 'local-install.sh'
      - 'uninstall.sh'
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Platypus
        run: |
          brew install platypus
          # Install command line tool
          sudo /usr/local/bin/platypus --install || true
          
      - name: Build Installer App
        run: |
          cd macos-installer
          chmod +x build-macos-app.sh
          ./build-macos-app.sh
          
      - name: Build Uninstaller App
        run: |
          cd macos-installer
          chmod +x build-uninstaller-app.sh
          ./build-uninstaller-app.sh
          
      - name: Import Code Signing Certificate
        if: github.event_name != 'pull_request' && env.MACOS_CERTIFICATE != ''
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          KEYCHAIN_PWD: ${{ secrets.KEYCHAIN_PWD }}
        run: |
          # Create a temporary keychain
          security create-keychain -p "$KEYCHAIN_PWD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PWD" build.keychain
          
          # Import certificate
          echo "$MACOS_CERTIFICATE" | base64 --decode > certificate.p12
          security import certificate.p12 -k build.keychain -P "$MACOS_CERTIFICATE_PWD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PWD" build.keychain
          
          rm certificate.p12
          
      - name: Sign Apps
        if: github.event_name != 'pull_request' && env.MACOS_CERTIFICATE != ''
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          SIGNING_IDENTITY: ${{ secrets.SIGNING_IDENTITY }}
        run: |
          if [ -n "$SIGNING_IDENTITY" ]; then
            echo "Signing MeticAI Installer.app..."
            codesign --force --deep --sign "$SIGNING_IDENTITY" "macos-installer/build/MeticAI Installer.app"
            
            echo "Signing MeticAI Uninstaller.app..."
            codesign --force --deep --sign "$SIGNING_IDENTITY" "macos-installer/build/MeticAI Uninstaller.app"
            
            echo "Verifying signatures..."
            codesign --verify --verbose "macos-installer/build/MeticAI Installer.app"
            codesign --verify --verbose "macos-installer/build/MeticAI Uninstaller.app"
          else
            echo "No signing identity provided, skipping code signing"
          fi

      - name: Create DMG
        run: |
          cd macos-installer
          hdiutil create -volname "MeticAI" -srcfolder build -ov -format UDZO "build/MeticAI.dmg"
          
      - name: Notarize DMG
        if: github.event_name != 'pull_request' && env.APPLE_ID != ''
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          if [ -n "$APPLE_ID" ] && [ -n "$APPLE_ID_PASSWORD" ] && [ -n "$APPLE_TEAM_ID" ]; then
            echo "Submitting for notarization..."
            xcrun notarytool submit "macos-installer/build/MeticAI.dmg" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_ID_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" \
              --wait
            
            echo "Stapling notarization ticket..."
            xcrun stapler staple "macos-installer/build/MeticAI.dmg"
          else
            echo "Apple credentials not provided, skipping notarization"
          fi

      - name: Upload DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeticAI-macOS-Installer
          path: macos-installer/build/MeticAI.dmg
          retention-days: 30

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Download DMG Artifact
        uses: actions/download-artifact@v4
        with:
          name: MeticAI-macOS-Installer
          path: ./
          
      - name: Get version from build script
        id: version
        run: |
          VERSION=$(grep 'VERSION=' macos-installer/build-macos-app.sh | head -1 | cut -d'"' -f2)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"
          
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: macos-installer-v${{ steps.version.outputs.version }}-${{ github.run_number }}
          name: macOS Installer v${{ steps.version.outputs.version }} (Build ${{ github.run_number }})
          body: |
            ## macOS Installer for MeticAI
            
            This release contains the macOS installer DMG for MeticAI.
            
            ### Installation
            1. Download `MeticAI.dmg`
            2. Open the DMG file
            3. Run "MeticAI Installer"
            4. Follow the on-screen prompts
            
            ### Uninstallation
            1. Open the DMG file
            2. Run "MeticAI Uninstaller"
            3. Follow the on-screen prompts
            
            ### Changes
            - Built from commit: ${{ github.sha }}
          files: MeticAI.dmg
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
